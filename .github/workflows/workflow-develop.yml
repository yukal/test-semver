name: Develop

on:
  push:
    branches-ignore:
      - main

jobs:
  lint-workflow:
    uses: ./.github/workflows/job-lint.yml

  test-workflow:
    needs: lint-workflow
    uses: ./.github/workflows/job-test.yml
